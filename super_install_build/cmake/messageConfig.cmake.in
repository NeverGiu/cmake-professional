# Step 1: Placeholder will using 'configure_package_config_file' command to replace itself.
@PACKAGE_INIT@

# Step 2: The auto-generation exported files for targets needs be included.
include("${CMAKE_CURRENT_LIST_DIR}/messageTargets.cmake")

# Step 3: Check libraries and executable files if contains 
# 'check_required_components' function by CMake.
check_required_components(
	"message-shared"
	"message-static"
	"message-hello-world_wDSO"
	"message-hello-world_wAR"
	)

# Step 4: Check if 'PkgConfig::UUID' is exist. If not exist, 
# we will research UUID library(only valid under non-WIN system).
# if(NOT WIN32)
#         if(NOT TARGET PkgConfig::UUID)
#                 find_package(PkgConfig REQUIRED QUIET)
#                 pkg_search_module(UUID REQUIRED uuid IMPORTED_TARGET)
#         endif()
# endif()
